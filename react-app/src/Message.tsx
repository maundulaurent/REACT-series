// pascal casing --capital letter in the start

function Message() {
    // JSX - javascript xml
    const name = 'Charles Muema'
    if (name)
        return <h1>Init Solutions by {name}</h1>;
    return <h2>Hi, name is False</h2>
}

export default Message;